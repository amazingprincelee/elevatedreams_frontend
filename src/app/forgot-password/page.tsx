import ChangePassword from '@/components/shared/auth-component/change-password'
import { FC } from 'react'

type Props = {}
const page: FC<Props> = ({}) => {
  return (
    <>
      <ChangePassword />
    </>
  )
}
export default page
